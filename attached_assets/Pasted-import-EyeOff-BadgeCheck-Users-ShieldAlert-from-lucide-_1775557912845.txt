import { EyeOff, BadgeCheck, Users, ShieldAlert } from "lucide-react";

const filters = [
  {
    icon: <EyeOff className="w-6 h-6" />,
    title: "Unwatched only",
    description:
      "Tracks videos you watch in this session. Cleared when you close this tab.",
    color: "text-teal-400",
    border: "border-teal-400/20",
  },
  {
    icon: <BadgeCheck className="w-6 h-6" />,
    title: "Established Creators",
    description:
      "100k+ subscribers, channel 12+ months old. Hidden subscriber counts excluded.",
    color: "text-blue-400",
    border: "border-blue-400/20",
  },
  {
    icon: <Users className="w-6 h-6" />,
    title: "Small creators only",
    description:
      "1,000 subscribers or fewer. Channels with hidden counts are excluded.",
    color: "text-purple-400",
    border: "border-purple-400/20",
  },
  {
    icon: <ShieldAlert className="w-6 h-6" />,
    title: "Exclude likely AI-generated",
    description:
      "Using local signal analysis. Cloud AI analysis activates when the backend is configured.",
    color: "text-orange-400",
    border: "border-orange-400/20",
  },
];

export default function Features() {
  return (
    <section id="features" className="py-20 px-6 bg-background">
      <div className="max-w-4xl mx-auto text-center mb-12">
        <p className="text-xs font-semibold tracking-widest text-muted-foreground uppercase mb-3">
          Controls
        </p>
        <h2 className="text-3xl font-bold mb-4">Focused search controls</h2>
        <p className="text-muted-foreground max-w-xl mx-auto">
          LucidBeam gives you a set of filters to shape your research session.
          No dark patterns. No hidden ranking adjustments.
        </p>
      </div>

      <div className="max-w-4xl mx-auto grid grid-cols-1 sm:grid-cols-2 gap-6">
        {filters.map((filter) => (
          <div
            key={filter.title}
            className={`rounded-xl border ${filter.border} bg-card p-6`}
          >
            <div className={`mb-3 ${filter.color}`}>{filter.icon}</div>
            <h3 className="font-semibold text-base mb-1">{filter.title}</h3>
            <p className="text-sm text-muted-foreground">{filter.description}</p>
          </div>
        ))}
      </div>

      <p className="text-center text-xs text-muted-foreground mt-8">
        Filter preferences are stored locally in your browser. LucidBeam does
        not transmit your filter choices to any server.
      </p>
    </section>
  );
}